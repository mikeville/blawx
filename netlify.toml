[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

[functions]
  node_bundler = "esbuild"
  included_files = ["server/prompts/voxel-loft.txt"]

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
