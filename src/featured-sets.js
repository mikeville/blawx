// Editorial selection is deliberately independent of Recently made ordering.
// Replace/add public result IDs here; missing selections fall through in order.
export const FEATURED_SET_IDS = Object.freeze([
  'example-dragon-lighthouse', 'example-cat', 'example-nostalgia',
]);
