Your masks have mechanical errors:

- front and top masks disagree on which columns are filled (they share the object's width): front has columns 0-15, top has columns 4-13 — make them identical
- side mask columns and top mask rows disagree on the object's depth: side has depth cells 1-6,8-9,15, top implies depth cells 5-10 — make them agree
- declared bounds say y:0-15 but the front mask occupies y 0-13 (y=0 is the bottom row) — make them agree
- declared bounds say z:0-5 but the side mask occupies depth 1-15 — make them agree
- the object should be at most 6 cells deep front-to-back, but the side mask currently spans 15 depth cells — redraw the side view as a depth cross-section, not a second profile of the object

Fix every error and re-emit your COMPLETE corrected answer in exactly the
original format: the bounds line, then exactly three fenced code blocks
labeled front, side, top. No other text.
