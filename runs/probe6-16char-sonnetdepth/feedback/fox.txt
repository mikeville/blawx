Your masks have mechanical errors:

- front and top masks disagree on which columns are filled (they share the object's width): front has columns 0-15, top has columns 0-13 — make them identical
- side mask columns and top mask rows disagree on the object's depth: side has depth cells 0-5, top implies depth cells 1-9 — make them agree
- declared bounds say x:0-12 but the front mask occupies columns 0-15 — make them agree
- declared bounds say y:0-15 but the front mask occupies y 0-13 (y=0 is the bottom row) — make them agree

Fix every error and re-emit your COMPLETE corrected answer in exactly the
original format: the bounds line, then exactly three fenced code blocks
labeled front, side, top. No other text.
