Your masks have mechanical errors:

- top mask rows 8-13: every row must be exactly 16 characters, using only "#" and "." — count the characters in each row
- front and top masks disagree on which columns are filled (they share the object's width): front has columns 0-15, top has columns 2-13 — make them identical
- side mask columns and top mask rows disagree on the object's depth: side has depth cells 0-13, top implies depth cells 2-9 — make them agree
- declared bounds say y:0-15 but the front mask occupies y 0-9 (y=0 is the bottom row) — make them agree
- declared bounds say z:0-9 but the side mask occupies depth 0-13 — make them agree
- the front mask has 2 empty cells enclosed inside the silhouette — masks must be SOLID: fill every cell inside the outline with "#"

Fix every error and re-emit your COMPLETE corrected answer in exactly the
original format: the bounds line, then exactly three fenced code blocks
labeled front, side, top. No other text.
