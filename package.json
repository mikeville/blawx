{
  "name": "blawx",
  "private": true,
  "version": "0.1.0",
  "description": "A local-first 3D brick and voxel generation demo.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5179 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173 --strictPort",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "quickjs-emscripten": "0.32.0",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "vite": "^7.1.5"
  }
}
